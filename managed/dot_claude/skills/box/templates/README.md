---
box_schema: 1.3
---

# Box: {{SLUG}}

_The head of the box. The static zone below is hand-written. The projected zone is regenerated by `box rollup` — don't hand-edit it. The README is an **index** over the items in `items/<id>/`, not a container of their bodies._

## The prize

{{INTENT}}

_What this box is for, and what "done" looks like. The line you return to when the work sprawls._

## Repo facts

{{REPO_FACTS}}

_The repo, branch, key paths, build/test commands — the stable facts a fresh session needs to orient. Filled at kickoff._

## Origin

{{ORIGIN}}

_How this box came to be: the live session it was created from, or the PR/issue it was seeded from. History, not status._

## Track

_The ordered list of **items** — the work, plural. Each line is `id · one-liner  `[state]``; the substance lives in `items/<id>/` (`spec.md` at needs-discovery, `plan.md` at ready). This is an **index**, not the bodies. A box is for **many** items — don't collapse it to one. The natural head is a **decomposition/design item** whose job is to understand the shape of the work and cut it into the other items._

- [ ] D1 · {{DECOMPOSITION_ITEM}} — understand the shape of the work and cut it into items  `[needs-discovery]`

_Then the work items it lists, e.g. `- [ ] 1 · <item one-liner>  `[stub]``. See `templates/plan-item.md` for the line format and states._

## Working layout

```
.
├── README.md          # this file — static top + projected index over items
├── items/             # item bodies — created on the first non-stub item
│   └── <id>/
│       ├── spec.md    # written at needs-discovery (what/why; open questions allowed)
│       └── plan.md    # written at ready (agent-actionable; zero open questions)
├── follow-ups/        # parked follow-ups, one F<id>.md per entry (appears on first `box park`)
│   └── F1.md
├── log/               # append-only events — created on the 2nd entry
├── handoffs/          # carry-forward prompts — created on the first handoff
└── archive/           # done/superseded items + docs; superseded docs carry a SUPERSEDED banner
```

---

<!-- BOX: BEGIN PROJECTED -->

## Where things stand

_Not yet rolled up. Run `box rollup` to populate._

### State

_None yet._

### Document map

_None yet._

### Next moves

_None yet._

### Open follow-ups

_None yet._

### Open questions

_None yet._

_Raised minus resolved: every `open-question-Q<n>` log event with no matching `question-resolved-Q<n>`. One bullet each, carrying its `Q`-ID — `- Q<n> — <question>  (raised <YYYY-MM-DD>, still open)`. These stay listed here until resolved — an undecided question is never hidden._

<!-- BOX: END PROJECTED -->
